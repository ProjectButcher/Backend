// This script is the one who creates a connection between TypeORM and Postgres
// You should use "ormconfig.json" to configure your connection

import { createConnection } from 'typeorm' // TypeORM is used to facilitate all database actions

createConnection()
